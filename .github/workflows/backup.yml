# DEPRECATED — questo workflow è stato sostituito da:
#   - backup_db.yml      → backup giornaliero completo (schema + dati)
#   - backup_storage.yml → backup settimanale dei media
#
# Lasciato qui come riferimento storico. NON verrà eseguito.

name: ⚠️ DEPRECATED - Old DB Backup

on:
  workflow_dispatch:
    # Solo esecuzione manuale, NON più schedulato

jobs:
  deprecated:
    name: Noop - workflow deprecato
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Questo workflow è stato sostituito da backup_db.yml e backup_storage.yml"
          echo "Non fare nulla."
